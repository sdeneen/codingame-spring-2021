export const NUM_DIRECTIONS = 6;
export const NUM_DAYS = 24;
/* The minimum number of days required for a full tree lifecycle, which includes seeding it, growing it to max size, and then completing it */
export const MIN_NUM_DAYS_IN_TREE_LIFECYCLE = 5;
